#include "main.h"
#include <stdio.h>

/**
 * add - This program adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: Always 0.
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
