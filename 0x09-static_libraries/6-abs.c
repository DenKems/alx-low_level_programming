#include "main.h"
#include <stdio.h>

/**
* _abs - This function computes the absolute value of an integer
* @n: The integer
* Return: Return 0 (Success)
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
